@model KPISolution.Models.ViewModels.KPI.KpiDetailsViewModel

@{
    ViewData["Title"] = "Chuyển đổi thành KRI";
}

<div class="container-fluid px-4">
    <h1 class="mt-4"><i class="bi bi-arrow-up-circle me-2"></i>Chuyển đổi thành KRI</h1>
    <div class="mb-4">
        <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-secondary">
            <i class="bi bi-arrow-left me-1"></i> Quay lại
        </a>
    </div>

    <div class="card mb-4">
        <div class="card-header">
            <h5 class="mb-0">Xác nhận chuyển đổi chỉ số kết quả (RI) thành chỉ số kết quả then chốt (KRI)</h5>
        </div>
        <div class="card-body">
            <div class="alert alert-info">
                <i class="bi bi-info-circle me-2"></i>Thao tác này sẽ tạo một <strong>Chỉ số kết quả then chốt
                    (KRI)</strong> mới dựa trên RI hiện tại.
                <br>RI hiện tại sẽ được đánh dấu là không hoạt động sau khi chuyển đổi.
                <br>KRI là những chỉ số kết quả đặc biệt quan trọng đối với tổ chức và được theo dõi ở cấp quản lý cao
                hơn.
            </div>

            <div class="row">
                <div class="col-md-6">
                    <div class="mb-3">
                        <label class="form-label fw-bold">Tên:</label>
                        <div>@Model.Name</div>
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-bold">Mã:</label>
                        <div>@Model.Code (sẽ được chuyển thành KRI-@Model.Code.Replace("RI-", ""))</div>
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-bold">Đơn vị:</label>
                        <div>@Model.MeasurementUnit</div>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="mb-3">
                        <label class="form-label fw-bold">Phòng ban:</label>
                        <div>@(Model.DepartmentName ?? "Không xác định")</div>
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-bold">Giá trị mục tiêu:</label>
                        <div>@(Model.TargetValue?.ToString("N2") ?? "Không xác định")</div>
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-bold">Hướng đo lường:</label>
                        <div>@Model.MeasurementDirection</div>
                    </div>
                </div>
            </div>

            <div class="mb-3">
                <label class="form-label fw-bold">Mô tả:</label>
                <div>@Model.Description</div>
            </div>

            <form asp-action="PromoteToKri" method="post">
                <input type="hidden" asp-for="Id" />
                <div class="d-flex justify-content-end mt-4">
                    <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-outline-secondary me-2">Hủy</a>
                    <button type="submit" class="btn btn-success">
                        <i class="bi bi-arrow-up-circle me-1"></i>Xác nhận chuyển đổi
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>