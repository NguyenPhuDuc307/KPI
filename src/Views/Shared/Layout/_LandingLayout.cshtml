<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Deha KPI Management</title>
    <link rel="icon" href="/img/logo.png" type="image/x-icon">

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&family=Poppins:wght@300;400;500;600;700&display=swap"
        rel="stylesheet">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css">

    <!-- Custom CSS -->
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />

    @await RenderSectionAsync("Styles", required: false)
</head>

<body>
    <!-- Navigation -->
    <header>
        <nav class="navbar navbar-expand-lg bg-primary fixed-top">
            <div class="container">
                <!-- Logo -->
                <a class="navbar-brand text-white d-flex align-items-center" asp-controller="Landing"
                    asp-action="Index">
                    <div class="bg-white rounded d-flex align-items-center justify-content-center"
                        style="height: 36px; width: auto; padding: 0 10px; margin-right: 12px;">
                        <img src="/img/logo.png" alt="Logo" style="height: 24px; width: auto;">
                    </div>
                    KPI Management
                </a>

                <!-- Mobile Toggle -->
                <button class="navbar-toggler text-white" type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarNav" style="border-color: rgba(255, 255, 255, 0.5);">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <!-- Navigation Links -->
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav me-auto">
                        <li class="nav-item">
                            <a class="nav-link text-white" href="#features">
                                <i class="bi bi-grid-3x3-gap-fill me-1"></i>Tính năng
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-white" href="#process">
                                <i class="bi bi-arrow-repeat me-1"></i>Quy trình
                            </a>
                        </li>
                    </ul>
                    <div class="d-flex gap-2">
                        <a class="btn btn-outline-light" asp-area="Identity" asp-page="/Account/Login">
                            <i class="bi bi-box-arrow-in-right me-1"></i>Đăng nhập
                        </a>
                        <a class="btn btn-light text-primary" asp-controller="Home" asp-action="Index">
                            <i class="bi bi-speedometer2 me-1"></i>Truy cập hệ thống
                        </a>
                    </div>
                </div>
            </div>
        </nav>
    </header>

    <!-- Main Content -->
    <main role="main" class="mt-5 pt-3">
        @RenderBody()
    </main>

    <!-- Footer -->
    <footer class="py-5 mt-auto bg-light">
        <div class="container">
            <div class="row">
                <div class="col-md-4 mb-4 mb-md-0">
                    <div class="d-flex align-items-center mb-3">
                        <div class="bg-white rounded d-flex align-items-center justify-content-center shadow-sm"
                            style="height: 40px; width: auto; padding: 0 12px;">
                            <img src="/img/logo.png" alt="Logo" style="height: 28px; width: auto;">
                        </div>
                        <h5 class="fw-bold mb-0 ms-2">Deha KPI Management</h5>
                    </div>
                    <p class="text-muted">Hệ thống quản lý hiệu suất doanh nghiệp toàn diện giúp tổ chức của bạn đạt
                        được các mục tiêu kinh doanh.</p>
                </div>
                <div class="col-md-2 mb-4 mb-md-0">
                    <h6 class="fw-bold mb-3">Hệ thống</h6>
                    <ul class="list-unstyled">
                        <li class="mb-2"><a href="#" class="text-decoration-none text-muted">Trang chủ</a></li>
                        <li class="mb-2"><a href="#features" class="text-decoration-none text-muted">Tính năng</a></li>
                        <li class="mb-2"><a href="#" class="text-decoration-none text-muted">Hướng dẫn</a></li>
                        <li class="mb-2"><a href="#" class="text-decoration-none text-muted">Liên hệ</a></li>
                    </ul>
                </div>
                <div class="col-md-2 mb-4 mb-md-0">
                    <h6 class="fw-bold mb-3">Pháp lý</h6>
                    <ul class="list-unstyled">
                        <li class="mb-2"><a href="#" class="text-decoration-none text-muted">Điều khoản sử dụng</a></li>
                        <li class="mb-2"><a href="#" class="text-decoration-none text-muted">Chính sách bảo mật</a></li>
                        <li class="mb-2"><a href="#" class="text-decoration-none text-muted">GDPR</a></li>
                    </ul>
                </div>
                <div class="col-md-4">
                    <h6 class="fw-bold mb-3">Liên hệ</h6>
                    <ul class="list-unstyled">
                        <li class="mb-2"><i class="bi bi-geo-alt me-2"></i>Deha Software, Quận 1, TP. Hồ Chí Minh</li>
                        <li class="mb-2"><i class="bi bi-envelope me-2"></i>info@dehasoft.com</li>
                        <li class="mb-2"><i class="bi bi-telephone me-2"></i>(+84) 123-456-789</li>
                    </ul>
                    <div class="mt-3">
                        <a href="#" class="text-decoration-none me-2"><i class="bi bi-facebook fs-5"></i></a>
                        <a href="#" class="text-decoration-none me-2"><i class="bi bi-twitter fs-5"></i></a>
                        <a href="#" class="text-decoration-none me-2"><i class="bi bi-linkedin fs-5"></i></a>
                        <a href="#" class="text-decoration-none me-2"><i class="bi bi-instagram fs-5"></i></a>
                    </div>
                </div>
            </div>
            <hr class="my-4">
            <div class="d-flex flex-column flex-md-row justify-content-between align-items-center">
                <div class="text-muted small">
                    &copy; @DateTime.Now.Year Deha KPI Management System. All rights reserved.
                </div>
                <div class="mt-3 mt-md-0">
                    <img src="/img/logo.png" alt="Logo" style="height: 30px;">
                </div>
            </div>
        </div>
    </footer>

    <!-- Scripts -->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                document.querySelector(this.getAttribute('href')).scrollIntoView({
                    behavior: 'smooth'
                });
            });
        });

        // Active link highlighting
        const sections = document.querySelectorAll('section');
        const navLinks = document.querySelectorAll('.navbar-nav .nav-link');

        window.addEventListener('scroll', () => {
            let current = '';
            sections.forEach(section => {
                const sectionTop = section.offsetTop;
                const sectionHeight = section.clientHeight;
                if (pageYOffset >= (sectionTop - sectionHeight / 3)) {
                    current = section.getAttribute('id');
                }
            });

            navLinks.forEach(link => {
                link.classList.remove('active');
                if (link.getAttribute('href') === `#${current}`) {
                    link.classList.add('active');
                }
            });
        });
    </script>

    @await RenderSectionAsync("Scripts", required: false)
</body>

</html>